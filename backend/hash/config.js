var path = require('path');
var config = {
    debug: true,
    base_url : 'http://localhost',
	mysql: {
		host: '127.0.0.1',
		user: 'root',
        password : '',
		database: 'quickbit'
    }
}
module.exports = config